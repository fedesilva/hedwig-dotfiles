[user]
	name = fedesilva
	email = federico.silva@flyzipline.com
[core]
	editor = vim
	quotepath = false
	excludesFile = ~/.gitignore
[color]
	ui = true

[alias]
	co = checkout
	ci = commit -v
	cia = commit -av
	st = status
	br = branch
	bra = branch -a
  lg = log -p
  lol = log --graph --decorate --pretty=oneline --abbrev-commit
  lola = log --graph --decorate --pretty=oneline --abbrev-commit --all
  ls = ls-files
	append = town append
	hack = town hack
	kill = town kill
	new-pull-request = town new-pull-request
	prepend = town prepend
	prune-branches = town prune-branches
	rename-branch = town rename-branch
	repo = town repo
	ship = town ship
	sync = town sync
